import React from 'react';
import Login from '../components/login/Log-in';

const LoginPage = () => {
    return (
        <React.Fragment>
            <Login></Login>
        </React.Fragment>
    );
}
export default LoginPage;
import { createTheme } from '@mui/material/styles';

const theme = createTheme();

export const drawerWidth = 260;
export const drawerClosedWidth = theme.spacing(8) + 3;
export const sideBarMenuPadding = theme.spacing(6.25);

export const sideBarMenuWidthOffset = 30;
export const sideBarMenuWidth = 60;
